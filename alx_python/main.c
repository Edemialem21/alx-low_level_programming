#include "monty.h"

/**
 * main - Entry point
 * @argc: Number of command line arguments
 * @argv: pointer to an array of string containing the cla
 * Return: return 0(Success)
*/


int main(int argc, char **argv)
{
    (void) argv;

    validate_arguments(argc);
    return (0);
}